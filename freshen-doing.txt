# Freshen WIP handoff

## 2026-08-01

Stashed as: `freshen 672bb50f: pre-existing WIP` (not applied, not dropped)

Stashed files:

- doing.txt — carries five notes from the 2026-07-26 pass. Three look closed
  (canary_repos.json is populated, the test_security.py import was fixed
  upstream in template 2.9.3, pytest-timeout is now a declared dependency).
  Next step: reopen the stash, delete the settled entries, and keep the one
  live question — whether tests/format/fixtures should be excluded from ruff
  format at the template level so a formatter stops rewriting test fixtures.
